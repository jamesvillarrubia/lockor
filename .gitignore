out/
node_modules/
.vscode-test/
*.vsix
.ovsx

# GitHub configuration with secrets (excluded from git)
.github/config/github-config.yml
.DS_Store
.env

# Design files (keep local but don't track in git)
*.sketch
